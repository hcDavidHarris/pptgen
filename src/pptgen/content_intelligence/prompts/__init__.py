"""Prompt definitions for the content intelligence layer — Phase 11B.

All prompt strings are defined here and nowhere else in the codebase.
External callers must use prompt_registry.get_prompt() or the named helpers.
"""
